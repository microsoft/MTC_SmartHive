@using umbraco.MacroEngines
@inherits DynamicNodeContext
@{
    @Model.AncestorsOrSelf().First().SiteName
    
    if (@Model.Level > 1)
    {
        @Html.Raw(" - ") @string.Join(" - ", @Model.AncestorsOrSelf().Where("Visible").Where("Level > 1").Pluck("Name"))
    }
}
